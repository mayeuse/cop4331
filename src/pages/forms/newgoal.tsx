import React from "react";
import { Form } from "react-router-dom";

export const CSS = {
  Container: 'goals-wrapper'
}

const GoalForm = async () => {
  
  return (
    <div className={CSS.Container}>
      <Form >
      
      </Form>
    </div>
  )
}

export default GoalForm